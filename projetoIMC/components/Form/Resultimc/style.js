import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    textResult:{
        fontSize: 20,
        color: '#333',
        textAlign: 'center',
        marginTop: 50,
        fontWeight: 'bold',
    },
    textImcResult:{
        fontSize: 48,
        marginTop: 14,
        color: '#ff0043',
        textAlign: 'center',
        fontWeight: 'bold',
    }
})

export default styles;